; Per-directory settings for PHP CGI/FPM when user_ini.filename is enabled.
; Defaults: 8 MiB compress tree cap (FZC_WEB_MAX_UPLOAD_BYTES), 80 MiB single .fzc extract (10× or FZC_WEB_MAX_EXTRACT_ARCHIVE_BYTES).
upload_max_filesize = 128M
post_max_size = 160M
