# Integrated convert × fractal_zip

Generated by `php benchmarks/run_convert_integrated_table_bench.php`

**Win** = same corpus, forward convert → `.fz` smaller than baseline, extract → reverse-convert matches originals.

## Verified wins (3)

| Corpus | Preprocess | Base fzc | Conv fzc | Δ | vs table ref | Round-trip |
|--------|------------|--------:|---------:|--:|-------------:|:----------:|
| test_files53_xlsx | **xlsx_to_csv** | 177,720 | 74,212 | **+103,508** | 74,167 (Δ−45) | OK |
| test_files61/00_source_png | **png_to_bmp** | 60,608 | 24,269 | **+36,339** | — | OK |
| test_files61_zip | **zip_to_7z** | 257,853 | 237,760 | **+20,093** | — | OK |

### Table-relevant case: XLSX → CSV

Your table lists **test_files53** (CSV) at **74,167** fzc. The same shop-export data as **XLSX** compresses to **177,720** fzc raw — but after **`xlsx_to_csv` preprocess**, `.fz` is **74,212** (essentially table parity), with verified **extract → csv_to_xlsx** round-trip.

**Hook rule:** when a folder literal is `.xlsx`/`.ods`, try **`xlsx_to_csv`** / **`ods_to_csv`** before fractal literal transforms.

### PNG grids

**`png_to_bmp`** on test_files61 PNG grids: **−60%** fzc, **`bmp_to_png`** restores all 7 files after extract. Do **not** use **`bmp_to_png`** on table BMP corpora (34, 64) — reversible but **inflates** fzc.

### ZIP literals

**`zip_to_7z`** on `raster_formats.zip`: **−20 KB** fzc, **`7z_to_zip`** restores archive after extract.

## Losses / skip

| Case | Result |
|------|--------|
| test_files34/64 `bmp_to_png` | Round-trip OK, **+98 / +674** fzc vs baseline |
| test_files53 `csv_to_json` | **+4,380** fzc, csv round-trip **FAIL** |

## Re-run

```bash
php /srv/http/fractal_zip/benchmarks/run_convert_integrated_table_bench.php
```

JSON: `benchmarks/.convert_integrated_table_bench.json`
